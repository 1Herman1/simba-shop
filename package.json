{
  "name": "simba-shop",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "workspaces": [
    "shared",
    "client"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=client",
    "build": "npm run build --workspace=shared && npm run build --workspace=client && rm -rf dist && cp -R client/dist dist && echo '--- output directory ---' && pwd && ls -1 dist",
    "preview": "npm run preview --workspace=client"
  }
}
